

info = {

        'General':[
                'Command \'Yast\' is an interface manager for OpenSuse system (user, network ...)',
                'Board eth0 is for local network, ip is given by firewall ',
                'Board eth1 is for public network, ip is fixed "158.64.9.106"',
                'By default, systemd which use command "systemctl" will be linked to all programs installed by zipper.',
                '       -> sudo zipper search apache2',
                '       -> sudo zipper installapache2',
                '       -> sudo zipper enable apache2',
                ],
                
        
        'Web Server':[
                'Path config file : /etc/apache2/httpd.conf',
                'Document Root: /srv/www/htdocs',
                'Site web config: /etc/apache2/vhosts.d/...',
                ],

        'Web Mail':[
                'Dovecot: Server IMap (Reception + storeage emails)',
                'Postfix: Responsible of sending email. Protocol SMTP',
                'Postfix: Responsible of sending email. Protocol SMTP',
                'Site web config: /etc/apache2/vhosts.d/...',
                ],

        }


backup = {      
        }